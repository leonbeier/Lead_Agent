{
  "name": "lead-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Automated lead discovery and outreach preparation pipeline for Apollo, Azure OpenAI, and HubSpot.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc --noEmit -p tsconfig.json",
    "watch:lead-run": "tsx scripts/watch-lead-run.ts",
    "watch:lead-loop": "tsx scripts/run-lead-loop.ts",
    "benchmark:lead": "tsx scripts/run-lead-benchmark.ts"
  },
  "dependencies": {
    "@azure/ai-projects": "^2.0.0",
    "@azure/identity": "^4.8.0",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "undici": "^7.16.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.17",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
